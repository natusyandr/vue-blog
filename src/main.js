import { createApp } from 'vue'
import App from './App.vue'
// router(при использовании vue-cli: vue add router - загрузка библиотеки_ подключение маршрутизатора
// npm: npm install vue-router@4 - загрузка библиотеки)
// vuex, vuetify,
// библиотеки со стилями
// глобальные настройки приложения (например методы)
createApp(App).mount('#app')
